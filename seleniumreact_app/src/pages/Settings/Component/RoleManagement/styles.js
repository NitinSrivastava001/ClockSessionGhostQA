import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    main: {
        margin: "20px",
    },
    header: {
        fontWeight: "400",
        fontSize: "14px",
        lineHeight: "21px",
        padding: "10px 22px",
    }
});